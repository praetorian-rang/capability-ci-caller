name: Release

on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  release:
    uses: praetorian-rang/capability-ci-callee/.github/workflows/release.yml@main
    with:
      environment: production
    permissions:
      contents: read
